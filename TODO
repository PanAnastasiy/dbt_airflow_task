Разделит таск группу( точные seed данные сделать seed данные

Из SNOWFLAKE_SAMPLE_DATA построить нормальный data vault

Сделать так чтобы была инкрементальная загрузка (initial up initial
сделать seed данные
Row vault из всех таблиц
Business vault ( обычный data vault + pit таблицы)
На выходе data mart который покажет какой-то красивый отчет
demention date, demention customer, чтобы сделать звезду одна фартовая таблицы и 3 demention
Сделат
Сделать Сидь за неделю
ом customer data frame сокмбинировать с хабом и стилетом, который уже существуют в data vault. Есть мастер Данные вне data vault, сделать так чтобы на buidness он комбинировал эти 2 ресурса и сделал сателит
Business vault raw + incremental load
Под должен собирать инфу по кастомеру
Задача со звездочкой написать один из сателитов так чтобы все атрибуты, которые часто меняются чтобы хранились в одном сателлите, а те что редко в другом вокруг кастомера
Линка должна соединять только 2 сущности. У линки может быть свой сателлит
Effectivity satellite

TODO:

NOT EXISTS С КОРРЕЛИРУЮЩИМ ЗАПРОСОМ ПО КАНОНУ ИСПОЛЬЗОВАТЬ NOT EXISTS ЛИНКА НА ORDER-Ы НЕ БРАТЬ НА ВЕСЬ ИНПУТ АУТПУТ ИНКРЕМЕНТ ПО ДАТЕ!

НЕ УЧАВСТВУЕТ RECORD_SOURCE
КЛЮЧ МОЖЕТ СО PK RECORD SOURCE
Исправить тупые запросы в Hub link sat

не прописывать весь код вшит, а в dockerfile
Вся логика в     Dockerfile

order ради приличия таблицу тоже можно было вставить
а то линка есть - а ведет в пустоту

