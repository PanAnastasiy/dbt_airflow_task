Разделит таск группу( точные seed данные сделать seed данные

Из SNOWFLAKE_SAMPLE_DATA построить нормальный data vault

Сделать так чтобы была инкрементальная загрузка (initial up initial
сделать seed данные
Row vault из всех таблиц
Business vault ( обычный data vault + pit таблицы)
На выходе data mart который покажет какой-то красивый отчет
demention date, demention customer, чтобы сделать звезду одна фартовая таблицы и 3 demention
Сделат
Сделать Сидь за неделю
ом customer data frame сокмбинировать с хабом и стилетом, который уже существуют в data vault. Есть мастер Данные вне data vault, сделать так чтобы на buidness он комбинировал эти 2 ресурса и сделал сателит
Business vault raw + incremental load
Под должен собирать инфу по кастомеру
Задача со звездочкой написать один из сателитов так чтобы все атрибуты, которые часто меняются чтобы хранились в одном сателлите, а те что редко в другом вокруг кастомера
Линка должна соединять только 2 сущности. У линки может быть свой сателлит
Effectivity satellite

TODO:

NOT EXISTS С КОРРЕЛИРУЮЩИМ ЗАПРОСОМ ПО КАНОНУ ИСПОЛЬЗОВАТЬ NOT EXISTS ЛИНКА НА ORDER-Ы НЕ БРАТЬ НА ВЕСЬ ИНПУТ АУТПУТ ИНКРЕМЕНТ ПО ДАТЕ!

НЕ УЧАВСТВУЕТ RECORD_SOURCE
КЛЮЧ МОЖЕТ СО PK RECORD SOURCE
Исправить тупые запросы в Hub link sat

не прописывать весь код вшит, а в dockerfile
Вся логика в     Dockerfile

order ради приличия таблицу тоже можно было вставить
а то линка есть - а ведет в пустоту

Источник hash где находится

Дата vault


Merge не работает что нужно сделать чтобы поджинить таблицы

Мне нужно соединть

Вставить хэш смысл хэша

Еффективный сателит  в bisnuess

Разделить сателлит хабы по папкам

Старая строка юзлес эффективти order здесь не нужен был эффективтити satelatet

Record source должен быть в hash

На них должен быт hash

Брать не линки, а из sattlite

Виды питов

Dense Rank
Spark Speead

Построить Stattelite а потом сделать pit поверх саттелита
Сателлит не должен быть поверх пита

САТЕЛЛИТ СТРОИТ БИЗНЕС-ЛОГИКУ МОЖЕТ БЫТЬ 3 САТЕЛИТА  INTEGRATED SATTELETE

ПИТ КОНЕЧНАЯ ТОЧКА ПИТА ИСПРАВИТЬ


Сделать разделение dim facts не инкрементал
Почему Pit понять пример почему у меня MAX стоит

Где merge в деменшинах

Raw vault Только insert

Strouse в хэше

Эффективти сателит на линку!!!!!!

Slow change tool проставить
В сателите грузим всё, всё что ,
Data mart dementions инкрементарная загрузка должна быть
Фильтрация по дате? Финт ушами сработает с hub,
Питы должны быть с парсом
Инкрементная загрузка в питах
Slowly change tips
Они нужны в dit

entrypoint.sh написать в

Поднят airflow
Init dag
Gfgjgjgj

Gfg

2 Обычных дага и Inceremntal даг
Даг который чистит
Даг который инициализирует
Если код запустил и данных нету - то Initial load snowflake
ЕСЛИ ЕСТЬ - incremental load

Один матьего даг первый инкрементарный и иницализирующий

Поднять airlflow

Второй даг делает клин ап

Merge в контексте

Merge input target insert target SQL В мердж МОЖНО ИСПОЛЬЗОВАТЬ КАК SQL ОПЕРАЦАИЮ В DATA VAULT В EFECTIVITY SATELIRT

DBT-PROJECT-TASK

ПЕРАЦИИ ПРИ ОКРЕМЕНТАЦИИ


ИНКРЕМЕНТАЛ

